spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lunch
    username: root
    password: gosemvhs1!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always

